---
const { icon, title, color } = Astro.props;
---



<div 
    class="flex flex-row gap-2 items-center rounded-full shadow-xl p-1 px-4 bg-gray-50 border-1" 
    style={`border-color: ${color};`}
>   
   <i class={`${icon} colored text-xl`}></i>
    <p class="text-xl">{title}</p>
</div>